/*
Package sector provides a simple way to do dependency injection in Go.
Tag `inject:"+"` means inject this field and tag `inject:"*"` means inject this field and all it's members.
*/
package sector
